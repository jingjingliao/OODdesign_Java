package problem0102;

/**
 * Boat class extends vehicle class, have the attributes of ID, averageSpeed, and maxSpeed
 */
public class Boat extends Vehicle {

  /**
   * @param ID ID
   * @param averageSpeed averageSpeed
   * @param maxSpeed maxSpeed
   */
  public Boat(String ID, Float averageSpeed, Float maxSpeed) {
    super(ID, averageSpeed, maxSpeed);
  }
}
