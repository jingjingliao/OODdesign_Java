package problem0102;

/**
 * Train class extends vehicle class, have the attributes of ID, averageSpeed, and maxSpeed
 */
public class Train extends Vehicle {

  /**
   * @param ID ID
   * @param averageSpeed averageSpeed
   * @param maxSpeed maxSpeed
   */
  public Train(String ID, Float averageSpeed, Float maxSpeed) {
    super(ID, averageSpeed, maxSpeed);
  }
}
