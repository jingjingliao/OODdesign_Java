package problem0102;

/**
 * Bus class extends vehicle class, have the attributes of ID, averageSpeed, and maxSpeed
 */
public class Bus extends Vehicle {

  /**
   * @param ID ID
   * @param averageSpeed averageSpeed
   * @param maxSpeed maxSpeed
   */
  public Bus(String ID, Float averageSpeed, Float maxSpeed) {
    super(ID, averageSpeed, maxSpeed);
  }
}
